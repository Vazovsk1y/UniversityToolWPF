using UniversityTool.Domain.Services.Base;

namespace UniversityTool.Domain.Services
{
    // The empty interfaces like that could extend the base interface functions.
    // In ViewModels when i would like to interact with some another window we will use type with interface similar to this.
    public interface IDepartamentAddWindowService : IBaseWindowService
    {
       
    }
}
